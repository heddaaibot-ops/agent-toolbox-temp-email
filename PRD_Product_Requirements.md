# 📋 Agent's Toolbox - 產品需求文檔（PRD）

> **項目名稱**: Agent's Toolbox
> **版本**: v1.0 MVP
> **撰寫日期**: 2026-02-04
> **撰寫者**: @piggyxbot
> **審核者**: Hedda 姐姐, @Heddaaibot
> **狀態**: ✅ 已確認

---

## 📖 目錄

1. [產品概述](#產品概述)
2. [市場定位](#市場定位)
3. [目標用戶](#目標用戶)
4. [核心價值主張](#核心價值主張)
5. [MVP 功能範圍](#mvp-功能範圍)
6. [用戶流程](#用戶流程)
7. [功能規格](#功能規格)
8. [技術要求](#技術要求)
9. [成功標準](#成功標準)
10. [未來規劃](#未來規劃)

---

## 📱 產品概述

### 什麼是 Agent's Toolbox？

**Agent's Toolbox** 是第一個基於 Monad 區塊鏈的去中心化 AI Agent 工具市場。我們為 AI Agent 提供「鏈上資源」，讓 Agent 可以通過智能合約，安全、透明、自主地購買和使用現實世界中的稀缺資源。

### 一句話描述

> **讓 AI Agent 用智能合約購買現實世界的工具和服務**

### MVP：一次性郵箱服務

我們的第一個工具是「一次性郵箱服務」。Agent 可以通過智能合約付費，獲取一個臨時的、可接收和回覆郵件的郵箱地址，用於完成各種服務的註冊、驗證和通訊。

---

## 🎯 市場定位

### 我們解決的問題

#### 問題 1：AI Agent 無法自主付費使用工具
**現狀**：
- AI Agent 需要使用工具（如臨時郵箱），但需要人類提供 API key、信用卡
- 需要人工審批、手動配置
- Agent 無法真正「自主」運作

**我們的解決方案**：
- Agent 直接用智能合約付費
- 無需 KYC、無需 API key、無需信用卡
- 完全自主、即時交易

#### 問題 2：現有臨時郵箱服務的局限
**現狀**：
- 免費服務不穩定、有廣告
- 付費服務（如 Mailinator）需要訂閱（$79/月）
- 大多數服務只能接收，不能發送
- 無法與區塊鏈集成

**我們的解決方案**：
- 按需付費（0.001 USDC/小時）
- 支援接收 + 讀取 + 回覆
- 原生區塊鏈集成
- 智能合約保證透明度

#### 問題 3：缺乏 Agent 友好的工具市場
**現狀**：
- 現有工具市場（Zapier MCP、Composio）需要人工配置
- 中心化平台、單點故障
- 無法真正去中心化運作

**我們的解決方案**：
- 完全去中心化
- Agent 友好的 API 設計
- 基於 Monad 高性能（10,000 TPS）

---

### 市場空白

根據我們的競品研究（[詳見競品研究報告](monad_hackathon_research.md)）：

- ✅ **唯一的「一次性服務 + 智能合約付費」組合**
- ✅ **唯一原生支援 x402 協議的工具市場**
- ✅ **唯一支援雙幣種（USDC + MON）的平台**

---

## 👥 目標用戶

### 主要用戶：AI Agent 開發者

#### Persona 1：獨立 AI Agent 開發者
**背景**：
- 正在開發自主運作的 AI Agent
- Agent 需要註冊各種服務（Twitter、Discord、Web2 平台）
- 需要接收驗證碼、確認郵件

**痛點**：
- 不想手動為每個 Agent 創建郵箱
- 希望 Agent 能自主完成註冊流程
- 需要成本可控的解決方案

**使用場景**：
```
Agent 任務：「註冊一個 Twitter 帳號」
→ Agent 調用 Agent's Toolbox 智能合約
→ 付費購買臨時郵箱（0.001 USDC）
→ 使用郵箱註冊 Twitter
→ 接收驗證碼並完成註冊
→ 郵箱 1 小時後自動銷毀
```

#### Persona 2：企業 AI Agent 團隊
**背景**：
- 運行數百個 Agent 執行自動化任務
- 需要大量臨時郵箱用於測試、註冊、驗證

**痛點**：
- Mailinator 等服務按月訂閱（$79/月）太貴
- 需要更靈活的按需付費
- 需要可審計的支出記錄（區塊鏈）

**使用場景**：
```
企業運行 100 個 Agent 測試註冊流程
→ 每個 Agent 自主購買郵箱
→ 智能合約自動記錄所有交易
→ 企業可追蹤每筆支出（鏈上透明）
→ 成本可控：0.001 USDC × 100 = $0.1
```

#### Persona 3：Web3 AI Agent
**背景**：
- 運行在鏈上的 AI Agent
- 需要與 Web2 服務交互（如接收通知）

**痛點**：
- 無法持有信用卡、無法通過 KYC
- 需要完全去中心化的解決方案

**使用場景**：
```
鏈上 Agent 需要監控某個 Web2 服務
→ 註冊該服務需要郵箱驗證
→ Agent 用智能合約購買臨時郵箱
→ 完成註冊並接收通知郵件
→ 完全無需人工介入
```

---

### 次要用戶：人類開發者

雖然我們主要為 AI Agent 設計，但人類開發者也可以使用：

**使用場景**：
- 測試郵件功能
- 一次性註冊不信任的網站
- 保護個人隱私

---

## 💎 核心價值主張

### 對 AI Agent 的價值

1. **✅ 真正的自主性**
   - Agent 無需人工介入即可購買和使用工具
   - 智能合約自動執行付費邏輯
   - 無需 API key、信用卡、KYC

2. **✅ 成本透明且可控**
   - 按需付費（0.001 USDC/小時）
   - 所有交易鏈上可查
   - 無隱藏費用、無訂閱陷阱

3. **✅ 即時可用**
   - Monad 400ms 區塊時間
   - 智能合約確認後立即獲得郵箱
   - 無等待、無審批

4. **✅ 功能完整**
   - 接收郵件（基本功能）
   - 讀取內容（獲取驗證碼）
   - 回覆郵件（雙向通訊）

### 對開發者的價值

1. **✅ 簡化開發**
   - 簡單的 REST API
   - 完整的文檔和範例
   - 開箱即用

2. **✅ 可審計**
   - 所有交易鏈上記錄
   - 可追蹤 Agent 支出
   - 財務透明

3. **✅ 可擴展**
   - 支援大規模 Agent 部署
   - Monad 高性能（10,000 TPS）
   - 無瓶頸

---

## 🎯 MVP 功能範圍

### ✅ 包含的功能（Must Have）

#### 1. 智能合約：購買郵箱
**功能描述**：
- Agent 調用智能合約 `purchaseMailbox()` 函數
- 支援兩種付費方式：USDC 或 MON
- 指定郵箱有效期（如 3600 秒 = 1 小時）
- 合約發出 `EmailPurchased` 事件

**用戶故事**：
```
作為一個 AI Agent
我想要用智能合約購買一個臨時郵箱
以便我可以自主完成需要郵箱驗證的任務
```

**驗收標準**：
- [ ] Agent 可用 USDC 購買郵箱
- [ ] Agent 可用 MON 購買郵箱
- [ ] 合約正確記錄購買信息
- [ ] 合約發出 EmailPurchased 事件
- [ ] 單元測試覆蓋率 > 90%

---

#### 2. 後端：郵箱創建與管理
**功能描述**：
- 後端監聽 `EmailPurchased` 事件
- 調用 mail.tm API 創建實際郵箱
- 存儲郵箱信息到數據庫
- 設置過期定時器（自動銷毀）

**用戶故事**：
```
作為後端服務
我需要在 Agent 購買後立即創建實際郵箱
以便 Agent 可以馬上使用
```

**驗收標準**：
- [ ] 監聽到事件後 5 秒內創建郵箱
- [ ] 郵箱地址存儲到數據庫
- [ ] 過期時間正確設置
- [ ] 過期後自動銷毀郵箱

---

#### 3. API：查詢郵箱信息
**功能描述**：
- Agent 可通過 REST API 查詢郵箱信息
- 包括：郵箱地址、過期時間、狀態

**用戶故事**：
```
作為一個 AI Agent
我想要查詢我購買的郵箱信息
以便我知道郵箱地址和有效期
```

**API 端點**：
```http
GET /api/mailbox/:mailboxId

Response:
{
  "mailboxId": "abc123",
  "email": "abc123@mail.tm",
  "expiresAt": 1738771200,
  "active": true,
  "createdAt": 1738767600
}
```

**驗收標準**：
- [ ] API 返回正確的郵箱信息
- [ ] 處理郵箱不存在的情況（404）
- [ ] 處理郵箱已過期的情況
- [ ] 回應時間 < 200ms

---

#### 4. API：接收郵件
**功能描述**：
- 後端定期從 mail.tm 拉取新郵件
- 存儲到數據庫
- Agent 可查詢收件箱

**用戶故事**：
```
作為一個 AI Agent
我想要查看我的郵箱收到了哪些郵件
以便我可以讀取驗證碼或重要信息
```

**API 端點**：
```http
GET /api/mailbox/:mailboxId/emails

Response:
{
  "emails": [
    {
      "id": "email_001",
      "from": "noreply@twitter.com",
      "subject": "Verify your Twitter account",
      "preview": "Your verification code is...",
      "receivedAt": 1738770000
    }
  ],
  "total": 1
}
```

**驗收標準**：
- [ ] 新郵件在 30 秒內被拉取
- [ ] API 正確返回郵件列表
- [ ] 支援分頁（limit + offset）
- [ ] 郵件按時間倒序排列

---

#### 5. API：讀取郵件內容
**功能描述**：
- Agent 可讀取完整的郵件內容
- 包括：正文、HTML、純文本

**用戶故事**：
```
作為一個 AI Agent
我想要讀取郵件的完整內容
以便我可以提取驗證碼或其他重要信息
```

**API 端點**：
```http
GET /api/mailbox/:mailboxId/emails/:emailId

Response:
{
  "id": "email_001",
  "from": "noreply@twitter.com",
  "to": "abc123@mail.tm",
  "subject": "Verify your Twitter account",
  "text": "Your verification code is: 123456",
  "html": "<html>...</html>",
  "receivedAt": 1738770000
}
```

**驗收標準**：
- [ ] API 返回完整郵件內容
- [ ] 支援純文本和 HTML
- [ ] 處理郵件不存在的情況（404）
- [ ] 回應時間 < 300ms

---

#### 6. API：回覆郵件
**功能描述**：
- Agent 可通過 API 回覆郵件
- 使用 mail.tm API 發送郵件

**用戶故事**：
```
作為一個 AI Agent
我想要回覆收到的郵件
以便我可以與服務進行雙向通訊
```

**API 端點**：
```http
POST /api/mailbox/:mailboxId/reply

Request:
{
  "to": "support@example.com",
  "subject": "Re: Verification",
  "text": "Thank you for the verification code."
}

Response:
{
  "success": true,
  "messageId": "sent_001",
  "sentAt": 1738770100
}
```

**驗收標準**：
- [ ] 郵件成功發送
- [ ] 發送失敗有明確錯誤信息
- [ ] 支援純文本和 HTML
- [ ] 發送時間 < 5 秒

---

### ⏳ 不包含的功能（Nice to Have，未來版本）

#### 未來 Phase 2
- [ ] 郵件附件支援
- [ ] 郵件轉發功能
- [ ] 自定義郵箱域名
- [ ] 郵箱有效期延長

#### 未來 Phase 3
- [ ] 臨時手機號服務
- [ ] 驗證碼接收服務
- [ ] 代理 IP 服務
- [ ] 工具市場化（第三方提供者）

---

## 🔄 用戶流程

### 流程 1：Agent 購買並使用郵箱（完整流程）

```
┌─────────────────────────────────────────┐
│ Step 1: Agent 決定需要臨時郵箱           │
├─────────────────────────────────────────┤
│ 觸發場景：Agent 需要註冊 Twitter         │
│ Agent 分析：需要郵箱接收驗證碼           │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 2: Agent 調用智能合約              │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - Agent 準備 0.001 USDC                │
│ - 調用 purchaseMailbox(3600, USDC)    │
│ - 等待交易確認（~400ms on Monad）      │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 3: 智能合約執行                    │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - 檢查付費金額（0.001 USDC）           │
│ - 生成 mailboxId（唯一識別碼）          │
│ - 記錄購買信息到鏈上                    │
│ - 發出 EmailPurchased 事件             │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 4: 後端監聽到事件                   │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - ethers.js 監聽到 EmailPurchased       │
│ - 解析事件參數（buyer, mailboxId）      │
│ - 調用 mail.tm API 創建郵箱             │
│ - 存儲到數據庫（mailboxId → email）     │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 5: Agent 查詢郵箱信息               │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - GET /api/mailbox/{mailboxId}         │
│ - 獲得郵箱地址：abc123@mail.tm          │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 6: Agent 使用郵箱註冊 Twitter       │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - Agent 在 Twitter 註冊表單填寫郵箱     │
│ - Twitter 發送驗證碼到 abc123@mail.tm   │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 7: Agent 查詢收件箱                 │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - GET /api/mailbox/{mailboxId}/emails  │
│ - 發現新郵件：來自 Twitter               │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 8: Agent 讀取驗證碼                 │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - GET /api/mailbox/{mailboxId}/emails/1│
│ - 解析郵件內容：「Your code is: 123456」 │
│ - Agent 提取驗證碼：123456               │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 9: Agent 完成 Twitter 註冊          │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - Agent 在 Twitter 填寫驗證碼            │
│ - 註冊成功！                            │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Step 10: 郵箱過期自動銷毀                │
├─────────────────────────────────────────┤
│ Action:                                 │
│ - 1 小時後，後端定時器觸發              │
│ - 調用 mail.tm API 刪除郵箱             │
│ - 更新數據庫：active = false            │
└─────────────────────────────────────────┘
```

---

### 流程 2：Agent 回覆郵件（進階功能）

```
┌─────────────────────────────────────────┐
│ Agent 收到需要回覆的郵件                 │
├─────────────────────────────────────────┤
│ 場景：客服郵件、確認郵件等               │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ Agent 調用回覆 API                       │
├─────────────────────────────────────────┤
│ POST /api/mailbox/{id}/reply            │
│ {                                       │
│   "to": "support@example.com",          │
│   "subject": "Re: Support Request",     │
│   "text": "Thank you for your help."    │
│ }                                       │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ 後端調用 mail.tm API 發送郵件            │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│ 郵件成功發送                            │
│ 返回 messageId 給 Agent                 │
└─────────────────────────────────────────┘
```

---

## 📐 功能規格

### 智能合約規格

#### 合約：EmailService.sol

**狀態變數**：
```solidity
uint256 public priceInUSDC = 1000;     // 0.001 USDC (6 decimals)
uint256 public priceInMON = 1e16;       // 0.01 MON (18 decimals, 待調整)
address public usdcAddress;             // USDC 合約地址
address public owner;                   // 合約擁有者

mapping(address => Mailbox[]) public mailboxes;
```

**結構體**：
```solidity
struct Mailbox {
    address owner;        // Agent 錢包地址
    uint256 expiresAt;    // Unix timestamp
    bool active;          // 是否有效
    string mailboxId;     // 唯一識別碼
    string email;         // 實際郵箱地址
}
```

**事件**：
```solidity
event EmailPurchased(
    address indexed buyer,
    string mailboxId,
    string email,
    uint256 expiresAt,
    string paymentMethod  // "USDC" 或 "MON"
);
```

**函數**：

1. `purchaseMailbox(uint256 duration, address paymentToken, string mailboxId, string email)`
   - 訪問控制：public
   - 付費：USDC 或 MON
   - Gas 估計：~100,000 gas

2. `getMyMailboxes()`
   - 訪問控制：public view
   - 返回：Mailbox[]
   - Gas 估計：~50,000 gas

3. `setPrices(uint256 _priceInUSDC, uint256 _priceInMON)`
   - 訪問控制：onlyOwner
   - 用途：調整價格

4. `withdraw(address token)`
   - 訪問控制：onlyOwner
   - 用途：提取收益

---

### 後端 API 規格

#### 技術棧
- **語言**: TypeScript
- **框架**: Express.js
- **區塊鏈**: ethers.js v6
- **數據庫**: PostgreSQL
- **ORM**: Prisma
- **郵箱 API**: mail.tm

#### 數據庫 Schema

**Table: mailboxes**
```sql
CREATE TABLE mailboxes (
    id SERIAL PRIMARY KEY,
    mailbox_id VARCHAR(255) UNIQUE NOT NULL,
    owner_address VARCHAR(42) NOT NULL,
    email VARCHAR(255) NOT NULL,
    mail_tm_token TEXT,
    expires_at TIMESTAMP NOT NULL,
    active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP DEFAULT NOW(),
    INDEX idx_mailbox_id (mailbox_id),
    INDEX idx_owner (owner_address)
);
```

**Table: emails**
```sql
CREATE TABLE emails (
    id SERIAL PRIMARY KEY,
    mailbox_id VARCHAR(255) NOT NULL,
    email_id VARCHAR(255) UNIQUE NOT NULL,
    from_address VARCHAR(255),
    subject TEXT,
    text_body TEXT,
    html_body TEXT,
    received_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    FOREIGN KEY (mailbox_id) REFERENCES mailboxes(mailbox_id),
    INDEX idx_mailbox (mailbox_id),
    INDEX idx_received (received_at DESC)
);
```

---

#### API 端點規格

##### 1. GET /api/mailbox/:mailboxId

**描述**: 查詢郵箱信息

**請求**:
```http
GET /api/mailbox/abc123
```

**回應 200**:
```json
{
  "mailboxId": "abc123",
  "email": "abc123@mail.tm",
  "ownerAddress": "0x1234...",
  "expiresAt": 1738771200,
  "active": true,
  "createdAt": 1738767600
}
```

**錯誤回應 404**:
```json
{
  "error": "mailbox_not_found",
  "message": "Mailbox abc123 not found"
}
```

---

##### 2. GET /api/mailbox/:mailboxId/emails

**描述**: 查詢收件箱

**請求**:
```http
GET /api/mailbox/abc123/emails?limit=10&offset=0
```

**查詢參數**:
- `limit`: 每頁數量（預設 10，最大 100）
- `offset`: 偏移量（預設 0）

**回應 200**:
```json
{
  "emails": [
    {
      "id": "email_001",
      "from": "noreply@twitter.com",
      "subject": "Verify your Twitter account",
      "preview": "Your verification code is...",
      "receivedAt": 1738770000
    }
  ],
  "total": 1,
  "limit": 10,
  "offset": 0
}
```

---

##### 3. GET /api/mailbox/:mailboxId/emails/:emailId

**描述**: 讀取郵件完整內容

**請求**:
```http
GET /api/mailbox/abc123/emails/email_001
```

**回應 200**:
```json
{
  "id": "email_001",
  "from": "noreply@twitter.com",
  "to": "abc123@mail.tm",
  "subject": "Verify your Twitter account",
  "text": "Your verification code is: 123456\n\nPlease enter this code to verify your account.",
  "html": "<html><body>Your verification code is: <strong>123456</strong></body></html>",
  "receivedAt": 1738770000
}
```

**錯誤回應 404**:
```json
{
  "error": "email_not_found",
  "message": "Email email_001 not found in mailbox abc123"
}
```

---

##### 4. POST /api/mailbox/:mailboxId/reply

**描述**: 回覆郵件

**請求**:
```http
POST /api/mailbox/abc123/reply
Content-Type: application/json

{
  "to": "support@example.com",
  "subject": "Re: Support Request",
  "text": "Thank you for your help.",
  "html": "<html><body>Thank you for your help.</body></html>"
}
```

**回應 200**:
```json
{
  "success": true,
  "messageId": "sent_001",
  "sentAt": 1738770100
}
```

**錯誤回應 400**:
```json
{
  "error": "invalid_request",
  "message": "Missing required field: to"
}
```

**錯誤回應 403**:
```json
{
  "error": "mailbox_expired",
  "message": "Mailbox abc123 has expired"
}
```

---

### mail.tm API 集成

#### 創建郵箱
```javascript
// 1. 創建帳號
POST https://api.mail.tm/accounts
{
  "address": "abc123@mail.tm",
  "password": "random_secure_password"
}

// 2. 獲取 token
POST https://api.mail.tm/token
{
  "address": "abc123@mail.tm",
  "password": "random_secure_password"
}
→ 返回 JWT token

// 3. 存儲 token 到數據庫
```

#### 拉取郵件
```javascript
// 定時任務（每 30 秒）
GET https://api.mail.tm/messages
Authorization: Bearer {token}

→ 返回郵件列表
→ 存儲新郵件到數據庫
```

#### 發送郵件
```javascript
POST https://api.mail.tm/messages
Authorization: Bearer {token}
{
  "to": "support@example.com",
  "subject": "Re: Support Request",
  "text": "Thank you for your help."
}
```

---

## 🎯 成功標準

### Demo 展示成功標準

#### ✅ 技術功能
1. [ ] Agent 成功用 USDC 購買郵箱
2. [ ] Agent 成功用 MON 購買郵箱
3. [ ] 郵箱在 5 秒內創建完成
4. [ ] Agent 成功接收郵件
5. [ ] Agent 成功讀取郵件內容
6. [ ] Agent 成功回覆郵件
7. [ ] 郵箱過期後自動銷毀

#### ✅ 性能指標
- [ ] 智能合約確認時間 < 1 秒（Monad）
- [ ] API 查詢回應時間 < 200ms
- [ ] 郵件接收延遲 < 30 秒
- [ ] 郵件發送時間 < 5 秒

#### ✅ 可靠性
- [ ] 智能合約通過安全審計（自測）
- [ ] API 錯誤處理完善
- [ ] 數據庫事務正確性
- [ ] 無資金損失風險

---

### Hackathon 評審標準

根據 Monad Hackathon 評分標準：

#### 創新性（Innovation）
- ✅ 第一個「一次性服務 + 智能合約付費」平台
- ✅ 原生支援 x402 協議
- ✅ 雙幣種支援

#### 實用性（Utility）
- ✅ 解決真實問題（Agent 自主付費）
- ✅ 完整功能（接收 + 讀取 + 回覆）
- ✅ 可立即使用

#### 技術深度（Technical Depth）
- ✅ 智能合約設計合理
- ✅ 區塊鏈事件監聽
- ✅ 完整的後端架構

#### Monad 特性利用（Monad-Specific）
- ✅ 利用 Monad 高性能（10,000 TPS）
- ✅ 展示快速確認（400ms）
- ✅ EVM 兼容性

#### 完成度（Completeness）
- ✅ MVP 功能完整
- ✅ 文檔齊全
- ✅ 可部署到測試網

---

## 🚀 未來規劃

### Phase 2: 擴展工具類型（Q2 2026）

#### 新增工具 1：臨時手機號
**功能**：
- Agent 購買臨時手機號
- 接收 SMS 驗證碼
- 支援多國號碼

**價格**：0.01 USDC / 次

#### 新增工具 2：驗證碼接收服務
**功能**：
- 專門接收各種驗證碼（郵件、SMS、語音）
- 自動提取驗證碼
- 返回結構化數據

**價格**：0.005 USDC / 次

#### 新增工具 3：代理 IP 服務
**功能**：
- Agent 購買代理 IP
- 支援多地區
- HTTP/SOCKS5 協議

**價格**：0.1 USDC / 小時

---

### Phase 3: 工具市場化（Q3 2026）

#### 去中心化工具市場
**功能**：
- 第三方可上架自己的工具
- 智能合約自動分潤
- 工具評價系統
- 去中心化治理（DAO）

**商業模式**：
- 平台收取 5% 手續費
- 工具提供者獲得 95% 收益
- 用 $TOOL token 進行治理

---

### Phase 4: AI Agent 生態（Q4 2026）

#### Agent SDK
- 多語言 SDK（Python, JavaScript, Rust）
- 簡化 Agent 集成
- 自動處理付費邏輯

#### Agent 市場
- Agent 可以為其他 Agent 提供服務
- Agent-to-Agent 交易市場
- 基於 x402 協議的自動付費

---

## 📊 成功指標（KPI）

### MVP 階段（Hackathon）
- [ ] 部署到 Monad 測試網
- [ ] 成功 Demo 展示
- [ ] 獲得 Hackathon 獎金

### Launch 階段（3 個月內）
- [ ] 100 個 Agent 使用
- [ ] 1,000 筆郵箱交易
- [ ] $100 總交易額

### Growth 階段（6 個月內）
- [ ] 1,000 個 Agent 使用
- [ ] 10,000 筆交易
- [ ] $1,000 總交易額
- [ ] 新增 2 種工具類型

---

## 📝 附錄

### 相關文檔
- [競品研究報告](monad_hackathon_research.md)
- [最終決策文檔](FINAL_DECISIONS.md)
- [技術協調會議記錄](COORDINATION_SYNC.md)

### 參考資源
- [Monad 文檔](https://docs.monad.xyz)
- [mail.tm API](https://docs.mail.tm)
- [x402 協議](https://github.com/coinbase/x402)

---

**版本歷史**:
- v1.0 (2026-02-04): 初始版本，MVP 功能定義

**審核狀態**: ✅ Hedda 姐姐已確認
**下一步**: API 規範文檔撰寫

---

**撰寫者**: @piggyxbot
**審核者**: Hedda 姐姐, @Heddaaibot
**更新日期**: 2026-02-04

🩵 **Let's build the future for AI Agents!**
