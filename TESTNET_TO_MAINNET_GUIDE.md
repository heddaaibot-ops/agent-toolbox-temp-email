# 🔄 測試網到主網切換指南

> **項目**: Agent's Toolbox - EmailService
> **目的**: Hackathon Demo 時展示真實主網效果
> **策略**: 測試網驗證 → 主網部署 → Demo 展示

---

## 📋 執行計劃

### **階段 1：測試網開發 & 測試**（現在）✅

**目標**: 確保所有功能正常，沒有 bug

**步驟**：
1. ✅ 部署到 Monad 測試網
2. ✅ 測試所有功能（購買郵箱、USDC/MON 支付）
3. ✅ 後端 API 連接測試網
4. ✅ 端到端集成測試
5. ✅ 修復所有發現的 bug

**使用配置**：
```env
MONAD_RPC_URL=https://monad-testnet.drpc.org
CHAIN_ID=10143
```

---

### **階段 2：準備主網部署**（測試完成後）

**前置條件**：
- ✅ 測試網功能全部正常
- ✅ 沒有已知 bug
- ✅ 後端 API 運行穩定
- ⚠️ 準備好真實的 MON 代幣（購買或獲取）

**需要準備**：
1. **主網錢包**：
   - 創建新的 MetaMask 帳戶（或使用現有主網帳戶）
   - 確保有足夠的 MON 支付 Gas 費（預估 0.01-0.05 MON）

2. **真實 USDC**（可選）：
   - 如果主網有 USDC，可以使用真實 USDC
   - 否則我們會部署 Mock USDC（Demo 用也可以）

---

### **階段 3：切換到主網**（Hackathon Demo 前）

#### 📝 **Step 1: 更新 .env 文件**

編輯 `/Users/heddaai/clawd/Agent's Toolbox Hackathon/.env`：

```bash
# 註釋掉測試網配置
# MONAD_RPC_URL=https://monad-testnet.drpc.org
# CHAIN_ID=10143

# 啟用主網配置
MONAD_RPC_URL=https://monad-mainnet.drpc.org
CHAIN_ID=143

# 更新私鑰為主網錢包私鑰（⚠️ 確保有足夠 MON）
PRIVATE_KEY=0xYourMainnetPrivateKey
```

---

#### 🚀 **Step 2: 部署到主網**

```bash
# 1. 進入項目目錄
cd "/Users/heddaai/clawd/Agent's Toolbox Hackathon"

# 2. 加載環境變量
source .env

# 3. 模擬部署（檢查是否正常）
forge script script/Deploy.s.sol --rpc-url $MONAD_RPC_URL

# 4. 實際部署到主網
forge script script/Deploy.s.sol \
  --rpc-url $MONAD_RPC_URL \
  --private-key $PRIVATE_KEY \
  --broadcast \
  --verify

# 5. 等待部署完成
# ✅ 會顯示合約地址和 USDC 地址
# ✅ 自動生成 deployment.json
```

---

#### 📋 **Step 3: 更新後端配置**

部署完成後，更新後端 `.env` 文件：

```bash
# 後端項目路徑（妹妹創建的）
cd /path/to/backend

# 編輯 .env
nano .env
```

**更新以下變量**：
```env
# 從 deployment.json 複製
CONTRACT_ADDRESS=0xMainnetContractAddress
USDC_ADDRESS=0xMainnetUSDCAddress

# 切換到主網 RPC
MONAD_RPC_URL=https://monad-mainnet.drpc.org
CHAIN_ID=143
```

---

#### 🧪 **Step 4: 測試主網合約**

```bash
# 1. 查詢合約價格（確認部署成功）
cast call $CONTRACT_ADDRESS "pricePerHourMON()" \
  --rpc-url https://monad-mainnet.drpc.org

# 2. 購買一個測試郵箱（確認功能正常）
cast send $CONTRACT_ADDRESS \
  "purchaseMailbox(uint256,address)" \
  1 \
  0x0000000000000000000000000000000000000000 \
  --value 0.001ether \
  --rpc-url https://monad-mainnet.drpc.org \
  --private-key $PRIVATE_KEY

# 3. 查詢我的郵箱（確認購買成功）
cast call $CONTRACT_ADDRESS "getMyMailboxes()" \
  --from YOUR_ADDRESS \
  --rpc-url https://monad-mainnet.drpc.org
```

---

#### ✅ **Step 5: 驗證清單**

部署到主網後，確認以下項目：

- [ ] ✅ 合約成功部署到主網
- [ ] ✅ `deployment.json` 包含正確的主網地址
- [ ] ✅ 後端 `.env` 已更新為主網配置
- [ ] ✅ 後端服務可以監聽主網事件
- [ ] ✅ 前端（如果有）連接到主網合約
- [ ] ✅ 可以成功購買郵箱（測試交易）
- [ ] ✅ 後端可以調用 mail.tm 創建真實郵箱
- [ ] ✅ 端到端流程完整運行

---

### **階段 4：Hackathon Demo 展示**

#### 🎤 **Demo 時的賣點**

強調你們使用主網的優勢：

1. **真實區塊鏈**：
   > "我們的合約部署在 Monad 主網上，這不是測試網，是真實的區塊鏈！"

2. **實時交易**：
   > "你現在看到的每一筆交易都在 Monad 主網上真實發生。"

3. **可驗證性**：
   > "任何人都可以在 Monad 區塊鏈瀏覽器上查看我們的合約和交易記錄。"

4. **生產就緒**：
   > "這個 Demo 不是玩具，是真正可以上線運營的產品。"

---

#### 📊 **展示流程建議**

```
1. 打開 Monad 區塊鏈瀏覽器
   ├─ 展示合約地址
   └─ 顯示已有交易記錄

2. 實時購買郵箱
   ├─ MetaMask 彈出簽名請求（真實主網交易）
   ├─ 支付 MON 或 USDC
   └─ 交易上鏈（實時）

3. 展示生成的臨時郵箱
   ├─ 顯示唯一 mailboxId
   ├─ 展示過期時間
   └─ 實時接收郵件

4. 展示區塊鏈瀏覽器
   ├─ 交易已確認
   ├─ 事件日誌可見
   └─ 合約狀態更新
```

---

## ⚠️ 重要注意事項

### **安全提醒**

1. **私鑰安全**：
   - 主網私鑰務必妥善保管
   - 不要在公開場合展示私鑰
   - Demo 時可以事先準備好交易

2. **Gas 費準備**：
   - 確保錢包有足夠 MON（建議 0.1 MON 以上）
   - Demo 前先測試一筆交易

3. **備份計劃**：
   - 如果主網出問題，可以切回測試網
   - 準備好測試網的 `deployment.json` 備份

---

### **成本估算**

| 項目 | 預估成本 | 說明 |
|------|---------|------|
| 部署 EmailService | ~0.01 MON | 一次性 |
| 部署 Mock USDC | ~0.005 MON | 一次性（如果需要） |
| 測試交易（5 次） | ~0.005 MON | Demo 前測試 |
| **總計** | **~0.02 MON** | 約 $X USD（取決於 MON 價格） |

---

## 🔄 快速切換命令

### **測試網 → 主網**

```bash
# 備份測試網配置
cp .env .env.testnet.backup
cp deployment.json deployment.testnet.json

# 切換到主網配置
sed -i '' 's|monad-testnet|monad-mainnet|g' .env
sed -i '' 's|10143|143|g' .env

# 更新私鑰（手動）
nano .env  # 修改 PRIVATE_KEY

# 重新部署
source .env && forge script script/Deploy.s.sol \
  --rpc-url $MONAD_RPC_URL \
  --broadcast
```

---

### **主網 → 測試網**（如果需要回退）

```bash
# 恢復測試網配置
cp .env.testnet.backup .env
cp deployment.testnet.json deployment.json

# 重啟後端服務
cd backend && npm restart
```

---

## 📞 需要幫助？

如果切換過程中遇到問題：

1. **合約部署失敗**：
   - 檢查 MON 餘額是否足夠
   - 確認 RPC URL 是否正確
   - 查看錯誤信息

2. **後端無法連接**：
   - 確認 `.env` 文件更新了
   - 重啟後端服務
   - 檢查 RPC URL 可達性

3. **交易失敗**：
   - 確認 Gas 費設置
   - 檢查錢包授權
   - 查看區塊鏈瀏覽器

---

## ✅ 檢查清單：Hackathon 前一天

- [ ] 測試網功能全部正常 ✅
- [ ] 已購買足夠的主網 MON
- [ ] 主網錢包配置完成
- [ ] 已成功部署到主網
- [ ] 後端連接主網正常
- [ ] 至少完成 3 次端到端測試
- [ ] Demo 腳本準備完成
- [ ] 區塊鏈瀏覽器 URL 準備好

---

**準備好震撼評審了嗎？🚀**

記住：
> "We're not just building a demo, we're building a real product on Monad mainnet!"

---

**創建者**: @piggyxbot
**更新時間**: 2026-02-05
**狀態**: 準備從測試網到主網的完整遷移方案

🩵 **Let's make this Hackathon unforgettable!**
