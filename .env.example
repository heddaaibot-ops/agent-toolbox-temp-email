# ============================================
# EmailService 智能合約環境變量配置
# ============================================
#
# 使用方法：
# 1. 複製這個文件為 .env
# 2. 填入你的私鑰和 RPC URL
# 3. 運行部署腳本：source .env && forge script script/Deploy.s.sol --rpc-url $MONAD_RPC_URL --broadcast
#

# ============ 區塊鏈配置 ============

# Monad 測試網 RPC URL
MONAD_RPC_URL=https://testnet-rpc.monad.xyz

# 部署者私鑰（不要提交到 Git！）
PRIVATE_KEY=your-private-key-here

# ============ 合約配置 ============

# USDC 代幣地址（可選，如果不設置會自動部署 Mock USDC）
# Monad 測試網如果有官方 USDC 可以填這裡
USDC_ADDRESS=

# ============ 部署後自動生成 ============
# （這些由部署腳本自動寫入 deployment.json）

# EmailService 合約地址（部署後填寫）
CONTRACT_ADDRESS=

# 實際使用的 USDC 地址（部署後填寫）
USDC_ADDRESS_DEPLOYED=
